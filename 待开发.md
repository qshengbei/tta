# 待开发功能

## 1. 小程序订阅消息功能

### 功能描述

实现小程序订阅消息功能，通过微信官方渠道推送消息到用户微信，提高通知的及时性和触达率。

### 开发步骤

#### 1.1 申请订阅消息模板

- 登录微信公众平台（<https://mp.weixin.qq.com/）>
- 进入"功能" -> "订阅消息"
- 选择合适的模板类型，申请所需的订阅消息模板
- 记录模板ID，用于代码中调用

#### 1.2 修改云函数

- 更新 `sendNotification` 云函数中的 `sendWechatTemplateMessage` 函数
- 将其重命名为 `sendSubscribeMessage`
- 使用 `cloud.openapi.subscribeMessage.send` API 发送订阅消息
- 调整参数格式以匹配订阅消息的要求

#### 1.3 前端实现

- 在小程序中添加 `wx.requestSubscribeMessage` API 调用
- 向用户请求订阅权限
- 处理用户同意/拒绝的情况

#### 1.4 测试和优化

- 测试订阅消息的发送和接收
- 优化用户体验，确保订阅流程顺畅
- 处理各种异常情况

### 相关文件

- `cloudfunctions/sendNotification/index.js`
- 前端页面中需要请求订阅权限的组件

### 依赖条件

- 小程序服务类目变更为电商类型
- 申请到合适的订阅消息模板

### 预期效果

- 用户可以收到微信推送的实时通知
- 通知内容清晰明了，与本地通知保持一致
- 提高用户的参与度和满意度

<br />

## 2. 快递100推送接口实现

### 2.1 订阅推送接口

- 创建express100Callback云函数作为回调地址
- 实现签名验证逻辑
- 处理快递100推送的物流状态更新
- 更新数据库中的物流信息

### 2.2 地图轨迹推送接口

- 集成地图轨迹推送功能
- 实现地图轨迹数据的处理和存储
- 优化地图轨迹的显示效果

## 3. 其他待开发功能

- 通知详情页卡片信息显示
- 活动通知类型选择（系列、节日活动等）

<br />

## 4. 微信支付接口

### 功能描述

接入微信支付 API，实现用户下单后的真实支付能力，替换当前的模拟支付流程。

### 开发步骤

#### 4.1 调用统一下单接口

- 在支付云函数中调用 `cloud.cloudPay.unifiedOrder`（云开发支付）或微信支付 v3 的统一下单接口
- 传入订单号、金额、用户 openid、回调地址等参数
- 获取预支付交易会话标识（prepay_id）

#### 4.2 前端拉起微信官方收银台

- 小程序端调用 `wx.requestPayment`，传入 prepay_id、签名等参数
- **拉起微信官方收银台**，由用户在收银台内选择支付方式（微信零钱、银行卡等）
- 商户端无需自实现支付方式选择器，删除当前支付页面的自定义支付方式选择列表
- 用户在收银台内完成支付方式选择并输入密码完成支付

#### 4.3 从回调获取支付方式

- **bank_type 字段从微信支付的异步回调通知中获取**，而非商户前端手动传入
- 在支付成功回调通知中解析 `bank_type` 字段（如 `CFT` 表示零钱支付，其他为银行卡）
- 将 bank_type 保存到订单记录中，用于后续退款时的到账时间提示和退款状态流转
- 注意：前端 `wx.requestPayment` 的 success 回调仅表示用户完成了支付动作，真实支付结果和支付方式必须以后端接收的微信异步回调通知为准

### 相关文件

- `cloudfunctions/pay/index.js`（待创建或改造）
- `miniprogram/pages/pay/index.js`

### 依赖条件

- 小程序开通微信支付能力
- 商户号与小程序绑定完成

<br />

## 5. 微信退款接口

### 功能描述

接入微信支付退款 API，在售后流程中向用户原路退回资金，并引入"退款处理中"中间状态，避免银行卡退款时直接显示"退款完成"误导用户。

### 开发步骤

#### 5.1 增加退款中间状态

- 在 `refund_records` 集合的状态流转中增加 `processing`（退款处理中）状态
- 状态流转改为：`pending`（待退款）→ `processing`（退款处理中）→ `success`（退款成功）/ `failed`（退款失败/关闭）

#### 5.2 调用退款接口

- 在 `refund/index.js` 的 `handleProcessRefund` 函数中，调用 `cloud.cloudPay.refund` 或微信支付 v3 的申请退款接口
- 传入原订单号、退款单号、订单金额、退款金额
- 调用成功后将退款记录状态置为 `processing`，而非直接置为 `success`

#### 5.3 区分到账预期提示

- 根据订单记录中的 `bank_type` 字段显示不同的到账时间提示
- 微信零钱支付（`bank_type === 'CFT'`）：预计 5 分钟内到账
- 银行卡支付：预计 1-3 个工作日到账

#### 5.4 订单状态细化

- 银行卡退款在 `processing` 阶段，订单状态显示为"退款处理中"
- 退款确认成功后，订单状态才更新为"退款完成"

### 相关文件

- `cloudfunctions/refund/index.js`
- `cloudfunctions/schedule_refund/index.js`

### 依赖条件

- 微信支付接口已接入（依赖第 4 项）
- 商户账户余额充足

<br />

## 6. 退款结果回调通知

### 功能描述

接收微信支付退款结果的异步回调通知，准确感知退款何时到账，从而更新退款记录和订单状态为"退款完成"。

### 开发步骤

#### 6.1 创建退款回调云函数

- 新建 `refundCallback` 云函数，作为退款结果的接收地址
- 在调用退款接口时传入该云函数的 `notify_url`

#### 6.2 验签与解密

- 使用 APIv3 密钥对回调通知的 `resource.ciphertext` 进行验签和解密
- 解密后获取 `refund_status`（SUCCESS/PROCESSING/ABNORMAL/CLOSED）、`success_time`、`refund_recv_accout` 等字段

#### 6.3 更新退款记录与订单状态

- `refund_status === 'SUCCESS'`：退款记录置为 `success`，订单状态置为"退款完成"，售后状态置为"已完成"
- `refund_status === 'ABNORMAL'` 或 `'CLOSED'`：退款记录置为 `failed`，触发运营告警，等待人工处理

#### 6.4 主动查询兜底（可选）

- 若超过预期时间未收到回调通知，定时任务主动调用 `cloud.cloudPay.queryRefund` 或微信支付 v3 查询退款接口
- 推荐轮询策略：申请退款 1 分钟后查询一次，之后频率衰减（5 分钟、10 分钟、20 分钟、30 分钟）

### 相关文件

- `cloudfunctions/refundCallback/index.js`（待创建）
- `cloudfunctions/schedule_refund/index.js`（增加兜底查询逻辑）

### 依赖条件

- 微信退款接口已接入（依赖第 5 项）
- 配置 APIv3 密钥与回调地址

<br />

## 7. 批量申请售后

### 功能描述

支持在订单详情页一次选择多个商品批量提交售后申请，避免对多商品订单逐个发起售后的重复操作，提升用户体验。

### 现状

- **后端已支持批量**：`handleApplyAfterSalesOperation` 的 `params.items` 接收数组，可一次创建包含多个明细的售后单
- **前端仅支持单商品**：商品卡片上的「申请售后」按钮只能针对单个商品发起，弹窗中仅展示 `order.products[selectedProductIndex]` 一个商品

### 开发步骤

#### 7.1 新增批量申请入口

- 在订单详情页底部操作区或商品列表上方，新增「批量申请售后」按钮
- 仅在订单存在多个可售后商品时显示（订单状态为 `completed`/`shipping`/`delivered`/`refund`，且至少有 2 个商品未处于进行中售后）

#### 7.2 商品多选列表

- 弹出批量申请售后弹窗，展示可售后的商品列表（带勾选框）
- 支持单选/全选，已存在进行中售后的商品置灰不可选
- 每个商品可独立选择售后类型（退货退款、仅退款、换货、未收到货退款）和售后原因
- 支持为每个商品独立设置申请数量（不超过可申请数量）

#### 7.3 统一提交

- 一次提交生成一个售后单（`after_sales_cases`），包含多个明细（`after_sales_case_items`）
- 复用现有后端 `handleApplyAfterSalesOperation`，无需修改后端逻辑
- 提交成功后跳转售后详情页

#### 7.4 操作日志

- 订单操作日志的 `reason` 中已支持拼接多个商品信息（本次已实现）
- 批量申请时，`detail.items` 数组将包含所有申请的商品

### 相关文件

- `miniprogram/pages/order-detail/index.js`（新增批量选择逻辑）
- `miniprogram/pages/order-detail/index.wxml`（新增批量申请弹窗）
- `miniprogram/pages/order-detail/index.wxss`（新增批量选择样式）
- `cloudfunctions/updateOrderStatus/index.js`（后端已支持，无需修改）

### 依赖条件

- 无额外依赖，后端批量能力已就绪

<br />

## 8. 消息通知卡片主子标题分级展示

### 功能描述

将 tabbar 消息页的通知卡片改为"主标题 + 子标题"的两级结构，让通知分类更清晰，避免不同性质的通知（订单状态变更、售后流程）混在一起难以区分。

### 现状

- 所有 `type: 'orderStatusChange'` 的通知都归到「订单状态变更」分类
- 卡片标题直接显示通知本身的 `title`（如"催发货通知"、"退款申请已通过"、"用户确认收到寄回商品"）
- 售后流程相关的通知与订单状态变更通知混在同一分类下，层次不清晰

### 设计目标

```
┌────────────────────────────────────────┐
│ 订单状态变更                  3条未读  │  ← 主标题：分类名
│ 催发货通知                  14:30    │  ← 子标题：具体通知
│ 用户催发货：订单 #123 共2件...     │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 售后通知                      2条未读  │  ← 主标题：分类名
│ 退款申请已通过              昨天    │  ← 子标题
│ 您的订单 #123 退款申请已通过...     │
└────────────────────────────────────────┘
```

### 开发步骤

#### 8.1 区分"售后通知"与"订单状态变更"

目前两者 `type` 均为 `orderStatusChange`，需通过 `title` 关键词二次分桶：

- title 包含以下关键词 → 售后通知：`售后`、`退款`、`退货`、`换货`、`寄回`、`验货`、`拦截`、`催发货`
- 其他 → 订单状态变更

#### 8.2 调整分类配置

在 `NOTIFICATION_CATEGORY_CONFIGS` 中新增售后通知分类，通过 `includeKeywords` / `excludeKeywords` 配置关键词过滤：

```javascript
{ 
  label: '订单状态变更', 
  rawTypes: ['orderStatusChange'], 
  excludeKeywords: ['售后', '退款', '退货', '换货', '寄回', '验货', '拦截', '催发货'],
  priority: 1 
},
{ 
  label: '售后通知', 
  rawTypes: ['orderStatusChange'], 
  includeKeywords: ['售后', '退款', '退货', '换货', '寄回', '验货', '拦截', '催发货'],
  priority: 1 
}
```

#### 8.3 前端分桶与查询策略

由于数据库层面 `type` 相同，无法用 where 直接过滤，可选方案：

- **方案A**：查询所有 `orderStatusChange` 通知后，前端按 `title` 关键词分桶（实现简单，但查询量略增）
- **方案B**：在 `notifications` 集合中新增 `category` 字段，后端保存通知时预先归类（需改后端，但查询更高效）

#### 8.4 卡片结构调整

- 主标题：分类名（"订单状态变更" / "售后通知"）
- 子标题：通知本身的 `title`（如"催发货通知"、"退款申请已通过"）
- 内容：通知的 `content`

### 相关文件

- `miniprogram/pages/message/index.js`（分类配置、分桶逻辑、查询策略）
- `miniprogram/pages/message/index.wxml`（卡片主子标题布局）
- `miniprogram/pages/message/index.wxss`（主子标题样式）
- `cloudfunctions/sendNotification/index.js`（若采用方案B，保存通知时预填 category 字段）

### 依赖条件

- 无额外依赖，纯前端改造（方案A）或前后端联动（方案B）

