# 待创建数据库索引

本文件记录数据库中待创建的索引，便于后续在云开发控制台手动创建或通过 MCP 工具创建。

---

## refund_records 表

### 背景

`schedule_refund` 和 `schedule_refund_callback` 两个定时任务每 5 分钟扫一次 `refund_records` 表，随着数据量增长出现慢查询告警。

**慢查询告警示例**（2026-08-26）：

```
操作类型: QueryV3
查询表: refund_records
查询条件: where status='pending'
排序: createTime 升序
limit: 100
请求耗时: 1272 毫秒
优化建议: 可以尝试结合业务需要对 status 字段增加索引
```

### 待创建索引

| 索引名 | 字段 | 覆盖的查询 | 来源代码 |
|-------|------|----------|---------|
| `status_createTime_idx` | `{status: 1, createTime: 1}` | `where status='pending'` + `orderBy('createTime', 'asc')` | [schedule_refund/index.js#L13-L19](file:///Users/xiexiaoqiong/WeChatProjects/tta/cloudfunctions/schedule_refund/index.js#L13-L19) |
| `status_processingTime_idx` | `{status: 1, processingTime: 1}` | `where status='processing'` + `orderBy('processingTime', 'asc')` | [schedule_refund_callback/index.js#L15-L21](file:///Users/xiexiaoqiong/WeChatProjects/tta/cloudfunctions/schedule_refund_callback/index.js#L15-L21) |

### 索引参数说明

- **MgoIsUnique**: false（非唯一索引）
- **Direction**: `1` 表示升序（与代码中 `orderBy('xxx', 'asc')` 一致）

### 创建方式

**方式一：云开发控制台**

1. 打开云开发控制台 → 数据库 → `refund_records` 集合
2. 切换到「索引管理」标签
3. 分别添加上述两个复合索引

**方式二：MCP 工具**

登录 cloudbase MCP 后调用 `writeNoSqlDatabaseStructure`：

```
action: updateCollection
collectionName: refund_records
updateOptions:
  CreateIndexes:
    - IndexName: status_createTime_idx
      MgoKeySchema:
        MgoIsUnique: false
        MgoIndexKeys:
          - {Name: status, Direction: "1"}
          - {Name: createTime, Direction: "1"}
    - IndexName: status_processingTime_idx
      MgoKeySchema:
        MgoIsUnique: false
        MgoIndexKeys:
          - {Name: status, Direction: "1"}
          - {Name: processingTime, Direction: "1"}
```

### 预期效果

- `schedule_refund` 定时任务查询从 1272ms 降至个位数毫秒级
- `schedule_refund_callback` 定时任务查询避免后续数据量增长后出现同类慢查询
- 复合索引相比单字段 `status` 索引：前缀匹配 status 等值，后缀直接走索引顺序避免内存排序

---

## 后续可能需要加索引的表（暂不创建）

如再次出现慢查询告警，可考虑以下表的索引方案：

| 表 | 高频查询字段 | 排序字段 | 备注 |
|----|------------|---------|------|
| `after_sales_cases` | `orderId` + `caseStatus` | `updatedAt` | 订单详情/售后列表关联查询 |
| `after_sales_case_items` | `caseId` | `updatedAt` | 售后明细查询 |
| `after_sales_logs` | `caseId` | `createdAt` | 售后详情页操作记录 |
| `order_operation_logs` | `orderId` | `operatedAtTs` | 订单详情页操作记录 |

---

## 历史记录

| 日期 | 操作 | 备注 |
|------|------|------|
| 2026-08-28 | 记录 refund_records 待创建索引 | 因暂不创建，记录备查 |
