# 待创建索引

## notifications 集合

### 索引1：openid + createdAt + isDelete

**用途**：消息页面通知查询优化

**索引字段**：
| 字段 | 排序 |
|------|------|
| openid | 升序 |
| createdAt | 降序 |
| isDelete | 升序 |

**查询语句示例**：
```javascript
db.collection('notifications').where({
  openid: 'xxx',
  isDelete: _.neq(true),
  type: 'orderStatusChange'
}).orderBy('createdAt', 'desc').get()
```

**创建链接**（在云开发控制台打开）：
```
cloud://createindex?env=cloud1-4gs2vu8c6544e586&collection=notifications&from=console&s=%5B%7B%22field%22%3A%22openid%22%2C%22type%22%3A1%7D%2C%7B%22field%22%3A%22createdAt%22%2C%22type%22%3A-1%7D%2C%7B%22field%22%3A%22isDelete%22%2C%22type%22%3A1%7D%5D
```

---

## products 集合

### 索引1：materialId + _id

**用途**：商品详情页同布料商品查询优化

**索引字段**：
| 字段 | 排序 |
|------|------|
| materialId | 升序 |
| _id | 降序 |

**查询语句示例**：
```javascript
db.collection('products').where({
  materialId: 'xxx'
}).orderBy('_id', 'desc').get()
```

**创建链接**（在云开发控制台打开）：
```
cloud://createindex?env=cloud1-4gs2vu8c6544e586&collection=products&from=console&s=%5B%7B%22field%22%3A%22materialId%22%2C%22type%22%3A1%7D%2C%7B%22field%22%3A%22_id%22%2C%22type%22%3A-1%7D%5D
```

**注意**：建立此索引后，可以考虑删除现有的 `idx_products_materialId` 单字段索引，因为它是此组合索引的前缀索引，会造成冗余。

---

## 创建步骤

1. 打开微信开发者工具
2. 切换到云开发控制台
3. 选择对应的集合（notifications / products）
4. 进入"索引管理"页面
5. 点击"添加索引"
6. 按照上述字段和排序配置
7. 等待索引创建完成（可能需要几分钟）