# TTA 商品图片多档位系统重构

## 一、开发目标

为 TTA 微信小程序建立完整、统一的商品图片多档位管理与展示体系。

目前商品原图普遍约为 4000×4000+ 像素、单张约 10MB。不能让商品列表、分类页等场景直接加载原图。

建立以下 4 个图片档位：

档位

名称

建议尺寸

目标文件大小

主要用途

L1

列表图

最长边 360px

50～100KB

首页、分类、搜索、商品列表、推荐等

L2

卡片/详情图

最长边 800px

100～250KB

商品详情主图、购物车等

L3

高清预览图

最长边 1600px

300～700KB

点击商品图片后的高清预览

L4

原图

保留原始尺寸

保留原始文件

原图查看、后台母版，不需要重新生成

注意：

1. 上述尺寸是“最长边”，不是强制裁剪成正方形。
2. 必须保持原始图片宽高比例。
3. 例如：
   - 原图 4032×4032 → L1 360×360 → L2 800×800 → L3 1600×1600
   - 原图 4032×3024 → L1 360×270 → L2 800×600 → L3 1600×1200
   - 原图 3000×4000 → L1 270×360 → L2 600×800 → L3 1200×1600
4. 不允许为了统一尺寸而拉伸图片。
5. 不允许为了生成缩略图而改变原图。
6. 原图必须永久保留，作为后续重新生成各档图片的母版。

***

# 二、商品上传流程

修改商品新增/编辑页面。

## 1. 上传商品图片

管理员选择商品图片后：

第一步直接上传：

> L4 原图

原图上传完成后，保存原始图片信息，例如：

- cloudPath
- 文件大小
- 原始宽度
- 原始高度
- 文件格式
- 文件名

原图是后续 L1/L2/L3 生成的唯一母版。

***

## 2. 上传后显示图片处理面板

每张商品图片都应该显示：

```
商品图片

┌─────────────────────┐
│                     │
│      原图预览        │
│                     │
└─────────────────────┘

L1 列表图
360 × 360
78 KB
[预览]

L2 卡片/详情图
800 × 800
183 KB
[预览]

L3 高清预览图
1600 × 1600
426 KB
[预览]

L4 原图
4032 × 4032
9.82 MB
[预览]

```

实际尺寸、文件大小必须从真实文件计算，不允许写死。

***

# 三、生成图片按钮

商品上传页面提供：

> 【生成各级图片】

点击后，根据当前原图生成：

- L1
- L2
- L3

L4 不需要生成，因为 L4 就是用户上传的原图。

建议显示生成状态：

```
等待生成
生成中...
生成成功
生成失败

```

如果已经生成过，再次点击可以：

> 根据当前 L4 原图重新生成 L1/L2/L3

这样以后如果修改压缩参数，也可以重新生成。

***

# 四、每一级图片都必须能够预览

管理员生成图片后，可以点击：

> \[预览]

预览实际生成的文件，而不是预览原图。

例如：

```
L1 → 点击预览 → 实际查看 L1 文件

L2 → 点击预览 → 实际查看 L2 文件

L3 → 点击预览 → 实际查看 L3 文件

L4 → 点击预览 → 实际查看原图

```

这样管理员可以在发布商品之前确认：

- 图片是否清晰
- 是否出现压缩异常
- 是否出现变形
- 是否出现裁切
- 是否出现旋转问题
- 是否出现明显画质损失

***

# 五、发布商品时的校验

点击：

> 发布商品

之前进行图片资源检查。

必须确认：

```
L4 原图存在
L1 已生成
L2 已生成
L3 已生成

```

如果某一级生成失败，不允许静默发布。

给管理员明确提示：

> 商品图片尚未全部生成，请完成图片处理后再发布。

如果产品设计上允许管理员强制发布，需要明确提供“继续发布”机制，但默认不允许缺少图片档位的商品进入正式商品库。

***

# 六、商品图片数据库结构

检查当前商品数据结构，在不破坏现有数据兼容性的前提下进行调整。

推荐将每张商品图片设计成类似：

```
{
  original: {
    cloudPath: "...",
    width: 4032,
    height: 4032,
    size: 10240000,
    format: "jpg"
  },

  l1: {
    cloudPath: "...",
    width: 360,
    height: 360,
    size: 78000,
    format: "webp"
  },

  l2: {
    cloudPath: "...",
    width: 800,
    height: 800,
    size: 183000,
    format: "webp"
  },

  l3: {
    cloudPath: "...",
    width: 1600,
    height: 1600,
    size: 426000,
    format: "webp"
  }
}

```

字段名称可以根据当前项目的数据结构调整，不要求机械照搬。

重点是：

> 商品数据必须能够明确知道 L1/L2/L3/L4 每一档实际对应的云存储文件。

不要仅仅保存一个原图 URL，然后让前端在不同页面自行猜测如何处理。

***

# 七、图片生成到底在哪里实现

重点检查当前 TTA 项目使用的微信云开发架构。

当前项目：

- 微信小程序
- wx.cloud
- 云函数
- 云存储
- 没有独立 Java 后端

需要根据当前实际项目能力选择最合理的图片生成方式。

## 推荐优先方案

不要让小程序前端直接承担大量图片压缩和转换工作。

推荐：

```
管理员上传原图
        ↓
小程序
        ↓
云存储保存 L4 原图
        ↓
调用云函数/图片处理能力
        ↓
从 L4 原图生成
        ↓
L1 / L2 / L3
        ↓
保存到云存储
        ↓
返回各档图片信息
        ↓
管理端显示尺寸、大小、预览

```

如果当前微信云开发环境已经提供合适的云存储图片处理/缩放能力，优先使用官方能力，避免为了简单的图片缩放额外引入第三方图片服务器。

如果当前环境的云函数运行环境能够稳定使用图片处理库，也可以评估在云函数中处理。

不要擅自引入独立服务器。

请先检查当前项目实际使用的云开发能力，再选择实现方式。

***

# 八、图片生成规则

生成 L1/L2/L3 时：

## 1. 保持原始比例

绝对禁止：

```
4000×3000
↓
360×360

```

这种直接强制拉伸。

正确：

```
4000×3000
↓
L1：360×270

↓
L2：800×600

↓
L3：1600×1200

```

## 2. 只限制最长边

规则：

```
L1 最大边 = 360px
L2 最大边 = 800px
L3 最大边 = 1600px

```

例如：

```
原图 4000×3000

L1：
360×270

L2：
800×600

L3：
1600×1200

```

例如：

```
原图 3000×4000

L1：
270×360

L2：
600×800

L3：
1200×1600

```

例如：

```
原图 4032×4032

L1：
360×360

L2：
800×800

L3：
1600×1600

```

***

# 九、图片格式与压缩

评估当前微信小程序和云开发环境的兼容性。

优先考虑：

> WebP

如果当前环境或现有项目对 WebP 存在兼容性问题，则使用 JPEG。

建议质量：

```
L1：质量约 75～82
L2：质量约 80～88
L3：质量约 80～90

```

目标文件大小：

```
L1：50～100KB
L2：100～250KB
L3：300～700KB

```

注意：

这些是目标范围，不是硬性要求。

不同图片的纹理、颜色、细节完全不同，不能为了强行达到某个 KB 而导致明显画质下降。

如果图片本身复杂，可以允许超过目标范围。

***

# 十、所有前台图片展示场景统一改造

这是本次需求的重点之一。

请全面检查 TTA 当前项目中所有商品图片展示位置。

不能只修改商品列表页面。

需要搜索整个项目所有：

- 商品图片
- 商品封面
- 商品主图
- 商品缩略图
- 商品卡片
- 商品详情图片
- 购物车商品图片
- 首页推荐商品
- 分类商品
- 搜索结果
- 收藏商品
- 订单商品
- 其他商品相关图片

根据实际 UI 场景选择正确档位。

总体规则：

```
商品列表 / 分类 / 搜索 / 首页商品卡片
→ L1

商品详情主图
→ L2

购物车 / 订单等小尺寸商品图片
→ L1 或 L2，根据实际显示尺寸选择

商品详情中的高清查看
→ L3

查看原图
→ L4

```

不要继续让普通商品列表直接读取 L4 原图。

***

# 十一、商品详情页

商品详情页主图：

> 使用 L2

即：

```
最长边 800px

```

用户点击主图：

```
L2
↓
进入图片预览
↓
加载 L3

```

预览使用：

> L3 1600px

不要一点击就加载 10MB 原图。

***

# 十二、高清预览中的“查看原图”

在图片预览界面提供：

> 【查看原图】

用户主动点击后才加载：

> L4 原图

可以显示：

> 查看原图 · 可能消耗较多流量

加载逻辑：

```
详情页
800px
 ↓
点击
1600px
 ↓
用户点击“查看原图”
 ↓
4000px+ 原图

```

这样原图不会参与正常商品浏览。

***

# 十三、商品多图

如果一个商品有多张图片：

例如：

```
图片1
图片2
图片3
图片4

```

每张图片都必须有：

```
L1
L2
L3
L4

```

不能只给第一张生成多档图片。

商品列表只读取对应图片的 L1。

详情页读取 L2。

高清预览读取对应图片的 L3。

查看原图读取对应图片的 L4。

***

# 十四、懒加载

商品列表中的图片必须继续使用合理的懒加载机制。

不要因为有了 L1 就一次加载整个商品列表的所有图片。

例如：

```
100个商品

```

用户刚打开页面时，只加载当前可视区域附近的 L1。

随着滚动再加载后续商品。

检查当前项目是否已经实现图片懒加载，如果已经实现，保留并确保与新的图片 URL 体系兼容。

***

# 十五、旧商品兼容

这是必须考虑的。

当前数据库中可能已经存在大量旧商品，而且旧商品可能只有：

```
原图

```

没有：

```
L1
L2
L3

```

不能因为新图片系统上线导致旧商品无法显示。

需要设计兼容策略。

建议：

```
新商品：
L1/L2/L3/L4 全部生成

旧商品：
如果不存在 L1/L2/L3
→ 暂时继续使用原有图片

```

同时最好提供后续批量生成机制。

如果当前后台已有商品管理功能，可以增加：

> 批量生成商品图片

用于逐步给旧商品生成 L1/L2/L3。

不要一次性无控制地同时处理几百张 10MB 原图，避免云函数超时、内存或并发问题。

***

# 十六、删除商品图片时的处理

检查商品图片删除逻辑。

删除商品或替换商品图片时，需要考虑：

```
L1
L2
L3
L4

```

四个文件之间的生命周期。

如果管理员删除某张商品图片：

应该同时清理对应的：

```
L1
L2
L3
L4

```

避免云存储产生大量孤儿文件。

但如果 L4 原图作为母版仍被其他业务引用，则必须先检查引用关系，不允许误删。

***

# 十七、文件命名与目录

设计统一、清晰的云存储目录。

例如：

```
products/
  {productId}/
    images/
      {imageId}/
        original.xxx
        l1.webp
        l2.webp
        l3.webp

```

或者根据当前项目已有目录规范进行调整。

重点：

1. L1/L2/L3/L4 必须容易区分。
2. 不要产生无法追踪来源的随机文件。
3. 同一张商品图片的四个档位必须能够明确关联。
4. 重新生成时不要产生大量旧文件垃圾。

***

# 十八、不要破坏现有商品业务

本次开发主要针对：

> 商品图片上传、处理、存储、展示

不要擅自修改：

- 商品价格
- SKU
- 库存
- 商品分类
- 商品规格
- 购物车逻辑
- 订单逻辑
- 售后逻辑
- 支付逻辑

除非图片档位改造确实需要调整相关数据结构。

***

# 十九、开发前必须先做代码审查

在开始修改之前，先检查当前项目：

1. 商品新增页面在哪里
2. 商品编辑页面在哪里
3. 商品图片上传逻辑在哪里
4. 商品数据库结构
5. 商品图片云存储结构
6. 云函数列表
7. 当前是否已经有图片压缩能力
8. 当前所有商品图片展示位置
9. 当前商品详情图片预览逻辑
10. 当前图片删除逻辑
11. 当前旧商品数据结构
12. 是否已有图片懒加载
13. 是否已有统一图片组件

先形成简短的代码现状分析，再开始修改。

如果项目已经有统一的商品图片组件，优先扩展这个组件，不要在几十个页面分别重复写 L1/L2/L3 判断。

***

# 二十、建议建立统一图片获取逻辑

最好建立统一的图片档位获取方式，例如：

```
getProductImage(image, 'list')
getProductImage(image, 'detail')
getProductImage(image, 'preview')
getProductImage(image, 'original')

```

或者根据项目现有架构设计等价方案。

目标是：

页面不应该到处出现：

```
image.l1.cloudPath
image.l2.cloudPath
image.l3.cloudPath
image.original.cloudPath

```

而应该由统一图片工具/组件管理。

例如：

```
list     → L1
detail   → L2
preview  → L3
original → L4

```

这样以后如果修改图片规格，只需要修改一处。

***

# 二十一、最终验收标准

完成后必须实际验证以下完整流程：

## 新商品

```
上传 4000×4000+、约10MB原图
        ↓
原图上传成功
        ↓
生成 L1
        ↓
生成 L2
        ↓
生成 L3
        ↓
四档图片均显示：
尺寸 + 文件大小
        ↓
分别预览
        ↓
确认无变形、无异常裁切
        ↓
发布商品

```

## 前台

验证：

```
首页 → L1
分类 → L1
搜索 → L1
商品卡片 → L1
商品详情 → L2
点击详情主图 → L3
查看原图 → L4

```

检查浏览器/开发者工具或小程序网络请求，确认：

> 商品列表没有偷偷加载 L4 原图。

***

# 二十二、最终要求

这不是单纯增加三个缩略图文件。

目标是建立 TTA 统一的：

> **商品图片上传 → 原图保存 → 多档图片生成 → 图片预览 → 商品发布 → 前台按场景加载 → 原图查看**

完整图片基础设施。

请优先复用当前 TTA 已有架构，不要为了实现图片处理而擅自引入独立后端服务器。

开发过程中如果发现微信云开发当前环境对某种图片处理方式存在限制，先基于实际项目能力选择可行方案，并说明原因，不要假设不存在的 API。

完成后请输出：

1. 修改了哪些文件
2. 新增了哪些云函数/工具/组件
3. 商品图片数据结构如何变化
4. L1/L2/L3/L4 的生成方式
5. 前台哪些页面改用了哪个档位
6. 旧商品如何兼容
7. 图片删除如何处理
8. 如何测试
9. 是否存在需要我手动配置的云开发/云存储设置

生成图片功能必须是独立的，是提供各个地方能够进行调用的，比如上传商品时调用，以后有其他地方需要用到也能够进行调用，比如banner图以后可能也需要进行调用，不能仅仅是在上传商品功能里。

