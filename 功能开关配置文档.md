# 功能开关配置文档

本文档记录小程序中通过常量值控制的功能开关，便于开发人员快速了解和配置各项功能。

---

## 目录

1. [订单列表加载策略开关](#订单列表加载策略开关)
2. [商品列表页面功能开关](#商品列表页面功能开关)
3. [开关说明模板](#开关说明模板)

---

## 订单列表加载策略开关

### 页面路径
`miniprogram/pages/order-list/index.js`

### 2. orderListLoadStrategy - 订单列表加载策略

#### 功能描述
控制订单列表页进入时的数据加载策略，影响数据准确性、加载速度和用户体验。

#### 配置位置
```javascript
data: {
  // 订单列表加载策略开关
  // 1 = 直接拉取数据库（数据准确，无闪烁）
  // 2 = 缓存先显示 + 后台异步对比（秒开，但可能闪烁）
  orderListLoadStrategy: 1,
}
```

#### 取值说明

| 值 | 策略 | 效果 |
|-----|------|------|
| `1` | 直接拉取数据库 | 每次 onShow 都调用 `fetchOrders()`，数据始终准确，无闪烁 |
| `2` | 缓存 + 异步对比 | 先显示本地缓存（秒开），后台查询时间戳对比，发现差异再刷新 |

#### 影响范围

**JS 逻辑影响**：
- `onShow()` 函数根据策略分流
- `switchStatus()` 标签切换函数也根据策略分流
- 策略1：
  - `onShow` 调用 `fetchOrders()`，跳过 `_quickShowFromCache()` 和 `_asyncCheckAndUpdate()`
  - `switchStatus` 切换标签直接调用 `fetchOrders()`，跳过缓存读取和 `_validateOrderCacheAsync()`
- 策略2：
  - `onShow` 调用 `_quickShowFromCache()` + `_asyncCheckAndUpdate()`（原逻辑保留）
  - `switchStatus` 切换标签优先读缓存秒显，再后台 `_validateOrderCacheAsync()` 异步校验

**用户体验影响**：
- 策略1：数据准确，无闪烁；每次进入页面或切换标签都有短暂 loading（100-300ms）
- 策略2：秒开，但缓存过期时会出现数据闪烁（如数量从 2→1）

#### 使用场景

| 场景 | 推荐配置 | 原因 |
|-----|---------|------|
| 个人用户订单量小 | `1` | 查询快，数据准确，无闪烁 |
| 订单量大，追求首屏速度 | `2` | 利用缓存秒开，但需接受闪烁 |
| 管理员可能操作订单 | `1` | 用户侧缓存无法跨用户同步，直接拉取更可靠 |
| 测试/调试 | `1` | 数据实时，便于验证 |

#### 设计背景

**为什么默认选择策略1？**

1. **缓存+异步对比反而增加查询次数**：
   - 策略2在缓存不一致时，需查询3次（时间戳对比 + count + 拉取第一页）
   - 策略1只需查询1次

2. **小程序无法跨用户同步缓存**：
   - 本地缓存（`wx.setStorageSync`）只存在用户设备上
   - 管理员发货、退款、定时任务等操作无法更新用户侧缓存
   - 需要 Redis 等共享缓存中间件才能实现跨用户同步（小程序架构不支持）

3. **个人用户订单量小**：
   - 通常几十到几百条订单，配合分页（每页18条），查询性能无压力

#### 历史变更

| 版本 | 变更内容 |
|-----|---------|
| v1.0 | 初始实现，采用缓存+异步对比策略 |
| v1.1 | 新增 `orderListLoadStrategy` 开关，默认值改为1（直接拉取） |
| v1.2 | `switchStatus` 标签切换也按策略分流：策略1直接拉取、策略2缓存优先 |

---

## 商品列表页面功能开关

### 页面路径
`pages/category/index.js`

### 1. enableScrollHideTopBar - 滑动隐藏顶部栏开关

#### 功能描述
控制商品列表页面的顶部栏（标签栏和/或排序栏）是否随滚动隐藏/显示。

#### 配置位置
```javascript
data: {
  enableScrollHideTopBar: false, // 功能开关
  scrollHideMode: 'tabs',        // 隐藏模式
  isTopBarVisible: true,         // 顶部栏当前可见状态
}
```

#### 取值说明

**enableScrollHideTopBar**：

| 值 | 效果 |
|-----|------|
| `true` | 开启滑动隐藏功能 |
| `false` | 关闭滑动隐藏功能（默认） |

**scrollHideMode（仅在 enableScrollHideTopBar=true 时生效）**：

| 值 | 效果 |
|-----|------|
| `'both'` | 隐藏标签栏和排序栏 |
| `'tabs'` | 只隐藏标签栏（保留排序栏）（默认） |
| `'sort'` | 只隐藏排序栏（保留标签栏） |

#### 影响范围

**WXML 布局影响**：
- 标签栏位置（`top` 属性）
- 排序栏位置（`top` 属性）
- 容器内边距（`padding-top` 属性）
- 滚动区域高度（`scroll-view` 的 `height` 属性）

**JS 逻辑影响**：
- `onProductsScroll()` 滚动监听事件处理逻辑

#### 使用场景

| 场景 | 推荐配置 | 原因 |
|-----|---------|------|
| 追求滑动流畅度 | `enableScrollHideTopBar: false` | 避免频繁重排导致的卡顿 |
| 追求屏幕空间利用率 | `enableScrollHideTopBar: true, scrollHideMode: 'both'` | 上滑时隐藏所有顶部栏，显示更多商品 |
| 需随时切换排序 | `enableScrollHideTopBar: true, scrollHideMode: 'tabs'` | 保留排序栏，隐藏标签栏 |
| 需随时切换标签 | `enableScrollHideTopBar: true, scrollHideMode: 'sort'` | 保留标签栏，隐藏排序栏 |
| 测试/调试 | `enableScrollHideTopBar: false` | 顶部栏始终可见，便于测试 |

#### 历史变更

| 版本 | 变更内容 |
|-----|---------|
| v1.0 | 初始实现，默认开启滑动隐藏整个顶部栏 |
| v1.1 | 添加开关控制，默认关闭滑动隐藏 |
| v1.2 | 添加 scrollHideMode 配置，支持三种隐藏模式 |

---

## 开关说明模板

以下为新增功能开关的文档模板：

```markdown
### X. [开关名称] - [功能描述]

#### 功能描述
[简要描述该开关控制的功能]

#### 配置位置
```javascript
// 文件路径：[文件路径]
data: {
  [开关名称]: [默认值],
}
```

#### 取值说明

| 值 | 效果 |
|-----|------|
| `true` | [开启时的效果] |
| `false` | [关闭时的效果] |

#### 影响范围

**WXML 布局影响**：
- [列出受影响的布局元素]

**JS 逻辑影响**：
- [列出受影响的函数/逻辑]

#### 使用场景

| 场景 | 推荐配置 | 原因 |
|-----|---------|------|
| [场景描述] | [推荐值] | [原因说明] |

#### 历史变更

| 版本 | 变更内容 |
|-----|---------|
| [版本号] | [变更描述] |
```

---

## 维护规范

### 添加新开关

1. 在对应页面的 `data` 对象中添加开关变量
2. 在 `onLoad` 或初始化位置设置默认值
3. 在逻辑代码中添加开关判断
4. 在本文档中添加开关说明

### 修改开关默认值

1. 修改代码中的默认值
2. 更新文档中的默认值说明
3. 在历史变更中记录修改

### 废弃开关

1. 在代码中标记为废弃（添加 `// @deprecated` 注释）
2. 在文档中添加废弃标记
3. 后续版本中移除相关代码

---

## 相关链接

- [商品列表页面代码](file:///Users/xiexiaoqiong/WeChatProjects/tta/miniprogram/pages/category/index.js)
- [小程序配置文件](file:///Users/xiexiaoqiong/WeChatProjects/tta/miniprogram/app.json)

---

**文档版本**: v1.2  
**创建日期**: 2026-06-09  
**维护人**: 开发团队